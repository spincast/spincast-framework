package org.spincast.testing.core;

import org.spincast.core.config.SpincastConfig;

/**
 * Interface for a testing version of {@link SpincastConfig}.
 */
public interface SpincastConfigTesting extends SpincastConfig {
    // nothing required
}
