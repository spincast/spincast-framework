package org.spincast.core.exchange;

/**
 * The unparamaterized interface for the default 
 * request context.
 */
public interface DefaultRequestContext extends RequestContext<DefaultRequestContext> {
    // nothing required
}
