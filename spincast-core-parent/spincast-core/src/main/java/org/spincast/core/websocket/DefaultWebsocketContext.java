package org.spincast.core.websocket;

/**
 * The unparamaterized interface for the default 
 * WebSocket context.
 */
public interface DefaultWebsocketContext extends WebsocketContext<DefaultWebsocketContext> {
    // nothing required
}
