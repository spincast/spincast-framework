package org.spincast.core.json;

/**
 * Flag that indicate that the <code>IJsonArray</code>
 * is immutable.
 */
public interface IJsonArrayImmutable extends Immutable, IJsonArray {
    // nothing required
}
